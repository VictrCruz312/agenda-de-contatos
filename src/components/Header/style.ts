import styled from "styled-components";

export const ContainerSearchBar = styled.div`
  display: flex;
  flex-direction: column;
  padding: 15px 0 2px;

  .checkboxSearch {
    margin: 0;
    span {
      font-size: 12px;
      font-weight: 700;
    }
  }
`;
